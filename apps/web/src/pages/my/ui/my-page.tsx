/** 마이그래빗 화면 자리(스텁). 실제 화면은 이후 이전 작업에서 채운다. */
export function MyPage() {
  return (
    <div className="flex flex-1 items-center justify-center py-20 text-body1-normal text-text-4">
      마이그래빗 화면 준비 중
    </div>
  );
}

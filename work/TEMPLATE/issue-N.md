---
work_id: 'FEAT-000'
sequence: 1
github_issue: null # 등록 후 GitHub Issue 번호로 교체
---

# FEAT-000 / Issue 1 — 제목

> 향후 `test-scenarios` 도입을 위한 예약 템플릿이다. **현재는 사용하지 않는다.**
> 도입 후에는 파일명을 `issue-{번호}.md`로 바꿔서 쓴다.
> `sequence`는 work task 내부 순번이고, `github_issue`는 저장소 전체의 GitHub Issue 번호다.
> 실행 이슈 하나는 GitHub Issue 하나와 정확히 연결한다.
> **이 문서는 시그니처(타입 선언)와 시나리오(자연어 문장)만 담는다. 구현 코드도, 테스트 코드도 여기 쓰지 않는다.**

## 확정된 시그니처

> 함수 본문은 비운다. 이름 · 파라미터 · 반환 타입만 확정한다.
> `[GATE]` 사용자 승인 후에 기록한다.

### 타입

```typescript
// apps/web/src/…
```

### 함수 / 훅

```typescript
// apps/web/src/…
```

### 컴포넌트 Props

```typescript
// apps/web/src/…
interface ComponentNameProps {}
```

### 에러 케이스

| 상황 | 처리 (throw / early return / 무시) |
| ---- | ---------------------------------- |

---

## 테스트 시나리오

> 형식: `[정상/경계/예외] 대상 — [조건]일 때 [동작]하면 [기대 결과]한다`
> 조건과 기대 결과에는 시그니처에서 확정한 **구체적 값**을 쓴다.
> `정상적으로` `올바르게` 같은 모호어는 쓰지 않는다 — 테스트로 변환이 불가능하다.

### 정상

- [ ] [정상] … — …하면 …한다

### 경계

- [ ] [경계] … — …하면 …한다

### 예외

- [ ] [예외] … — …하면 …한다

---

## AC 커버리지

> `issues.md`의 AC가 **빠짐없이** 시나리오로 커버되는지 대조한다.

| AC   | 커버하는 시나리오 |
| ---- | ----------------- |
| AC-1 |                   |

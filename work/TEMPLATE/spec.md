---
id: 'FEAT-000' # FEAT | MIG | REF | FIX | NAT | INFRA
title: '제목'
type: 'feature' # feature | migrate | refactor | design-fix | fix | infra
screen: '-' # 화면 ID. 해당 없으면 -
priority: 'medium' # high | medium | low
created: 'YYYY-MM-DD' # 최초 1회, 이후 불변
revised: 'YYYY-MM-DD' # Changelog 추가 시 갱신
---

# {ID} — {제목}

## 배경 · 목표

무엇을 왜 하는지 2~4문장. 사용자 가치 중심으로.

## 범위

이 작업에서 건드리는 화면 / 레이어 / 파일 범위.

## Out of Scope

**이번에 하지 않을 것을 구체적으로 나열한다.**
명시하지 않으면 AI가 맥락을 확장 해석해 범위를 넘긴다.

- ...

## 용어 정의 (Ubiquitous Language)

코드·문서·대화에서 같은 단어가 같은 것을 가리키도록 고정한다.

| 용어 | 정의 |
| ---- | ---- |
|      |      |

---

## 기술 결정 (ADR) <!-- FEAT- / NAT- 전용. MIG-/REF- 는 이 섹션을 삭제한다 -->

> `feature-planner` 2단계 산출물. 최소 3안을 비교한 뒤 고른 것을 여기 적는다.
> **단점 없는 ADR은 신뢰할 수 없다.** Consequences에 트레이드오프를 반드시 쓴다.

### {결정 제목}

**Context** — 이 결정이 필요한 맥락. 무엇을 풀고 있는가.

**Decision** — 무엇을 선택했는가.

**Alternatives** — 거부한 안과 **각각의 거부 이유**. 이유 없는 거부는 적지 않는다.

| 안  | 내용 | 거부 이유 |
| --- | ---- | --------- |

**Consequences** — 트레이드오프. 장점뿐 아니라 **단점도 반드시** 명시한다.
이 결정 때문에 앞으로 무엇이 어려워지는가.

---

## 현행 동작 기준선 <!-- MIG- / REF- 전용. 그 외 작업은 이 섹션을 삭제한다 -->

> `refactor-baseline` 스킬이 채운다.
> **legacy 코드를 읽고 "관찰 가능한 동작"만 적는다. 옳고 그름은 여기서 판단하지 않는다.**
> 코드 구조·구현 방식은 적지 않는다 — 동작만.

| #   | 동작 | 확인한 위치 (legacy 경로) |
| --- | ---- | ------------------------- |
| 1   |      |                           |

## 시안 대조 결과 <!-- design-diff 스킬이 채운다 -->

> Figma 시안 + 기획과 위 기준선을 대조한 결과.
> **PNG에서 픽셀값을 역산하지 않는다.** Figma Dev Mode / 디자인 토큰이 기준이다.

| #   | 항목 | 현행 | 시안 | 판정                          |
| --- | ---- | ---- | ---- | ----------------------------- |
| 1   |      |      |      | 고침 / 유지 / 나중(별도 작업) |

### 확인 필요

기획·Figma·라우트·API 계약이 서로 어긋나 **임의로 결정하면 안 되는 것**만 적는다.
없으면 `없음`.

- ...

---

## 확정 명세 · 검증 기준

> 위 대조가 끝나고 사용자가 판정한 뒤에 작성한다.
> **관찰 가능한 결과만** Given-When-Then으로 쓴다.
> 금지어(Then): `정상적으로` `적절히` `올바르게` `보여진다` `처리된다` `업데이트된다` `반영된다`
> — 결과값 없는 서술은 테스트로 1:1 변환이 불가능하다.

- [ ] **AC-1** (범위: 단위|통합)
      Given `구체적 데이터값`
      When `관찰 가능한 행동`
      Then `값·개수·문구`

---

## Changelog

> 3요소 필수: **요약 / 사유 / 연관 커밋**. 사유가 없으면 나중에 판단이 불가능하다.
> 본문에 취소선이나 "삭제됨" 표시를 남기지 않는다 — 본문은 항상 현 기준 최신 상태만 유지하고, 이력은 여기에 모은다.

| 날짜 | 요약 | 사유 | 연관 커밋 |
| ---- | ---- | ---- | --------- |
|      |      |      |           |

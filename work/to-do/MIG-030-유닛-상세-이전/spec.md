---
id: 'MIG-030'
title: '유닛 상세 이전 — 레슨 목록'
type: 'migrate'
screen: 'LRN-03' # Figma 프레임 이름. L.* 대응은 INFRA-031 에서 확정한다
priority: 'high'
created: '2026-09-16'
revised: '2026-09-16'
---

# MIG-030 — 유닛 상세 이전 (레슨 목록)

## 배경 · 목표

`/learning/units/$unitId` 를 구현한다. `MIG-028`(학습 상세 공통 셸)이 만든 `PageHeading` 위에
레슨 목록과 목적지 진입 카드를 얹는다. 라우트 스텁은 이미 `develop` 에 있다
(`component: () => null`, 주석 "LRN-03(레슨 목록)을 이전할 때까지 유닛 카드의 목적지만 제공한다").

이 화면은 학습 흐름의 분기점이라, **레슨 풀이 · 북마크 문제 · 오답 문제 · 개념노트 네 목적지로
나가는 유일한 진입점**이다. 화면 자체보다 목적지 네 곳의 경로 계약을 먼저 확정해야
후속 이전 작업이 서로 어긋나지 않는다.

Figma 시안: [LRN-03 (node 13758-85559)](https://www.figma.com/design/hu4c6qCEMB62qHXk2v8Gsl/-New-Gravit?node-id=13758-85559)

## 범위

- 유닛 상세 화면 본체 — `pages/unit-detail/`
- 레슨 표시 컴포넌트와 조회 어댑터 — `entities/learning/` 에 추가
- `Chip` — `shared/ui/chip/` **신설**. 시안 3 variant 를 모두 정의하되 이번에 쓰는 것은 2종
- 목적지 4종의 **경로 계약 확정과 빈 라우트** — 경로·파라미터·레이아웃 그룹만 맞추고 본체는 비운다

## Out of Scope

- **문제 풀이 화면 3종의 내부 구현** (레슨 · 북마크 · 오답). legacy 기준 quiz feature +
  learning-widget 합계 약 1,500줄의 별도 서브시스템이며 각각 독립 `MIG-` 작업이다
- **개념노트 화면 내부 구현** — 마찬가지로 빈 라우트까지만
- **유닛 순번(`Unit01`) 확정** — 아래 확인 필요 1. 사용자가 서버 쪽을 확인 중이다
- **`학습 중` 칩 3종화** — API 에 데이터가 없다. 확인 필요 2
- **토큰 신설·정정** — 확인 필요 5. 이번에는 기존 토큰만 쓴다
- **에러 화면** — 전 화면 공통 관심사다. 확인 필요 4

## 용어 정의 (Ubiquitous Language)

| 용어       | 정의                                                                          |
| ---------- | ----------------------------------------------------------------------------- |
| 챕터       | 유닛의 상위 묶음. `chapterId` 로 식별                                         |
| 유닛       | 레슨의 상위 묶음. `unitId` 로 식별. **이 화면의 주인공**                      |
| 레슨       | 문제 묶음 하나. `lessonId` 로 식별. 풀이의 최소 단위                          |
| 유닛 순번  | 화면에 보이는 `Unit01` 의 `01`. **챕터 안에서의 순서**이지 `unitId` 가 아니다 |
| 목적지 4종 | 레슨 풀이 · 북마크 문제 · 오답 문제 · 개념노트                                |
| 풀이 화면  | 문제를 실제로 푸는 화면 3종. 개념노트는 풀이가 아니다                         |

---

## 현행 동작 기준선 (legacy)

> `apps/legacy-web` 을 읽고 관찰 가능한 동작만 기록한다. 기준일 2026-09-16.
> **legacy 의 경로 구조는 기준선이 아니다** — `develop` 이 이미 단일 ID 로 통일했다 (아래 §라우트 계약).

### A. 화면 내용

| #   | 동작                                                                                     | 확인한 위치 (legacy)                          |
| --- | ---------------------------------------------------------------------------------------- | --------------------------------------------- |
| A1  | 경로 표시는 `홈` → `{챕터명}` → `Unit{NN}` 3단계. **넓은 화면에서만** 보인다             | `entities/learning/ui/summary-header.tsx`     |
| A2  | 제목은 `Unit{NN}`. 유닛명은 나오지 않는다                                                | `learning/$chapterId/$unitId/index.tsx`       |
| A3  | `NN` 은 **챕터 유닛 목록에서 이 유닛의 순번**(index + 1)을 2자리로 0 채운 값이다         | 〃 + `shared/lib/format-unit-index.ts`        |
| A4  | 제목 아래 설명은 레슨 목록 응답의 유닛 설명이다                                          | 〃                                            |
| A5  | `개념노트` 카드가 하나, 그 아래 `북마크` · `오답노트` 카드가 둘 있다                     | 〃                                            |
| A6  | `북마크` 설명은 `북마크한 문제를 풀어요.`, `오답노트` 설명은 `틀린 문제를 복습해요.`     | 〃                                            |
| A7  | 세 카드는 **항상 활성**이다. 풀 문제가 없어도 비활성 표시나 안내가 없다                  | 〃 (분기 자체가 없다)                         |
| A8  | `문제 리스트` 섹션에 레슨이 나열된다. 각 항목에 레슨 제목 · `{n}문제` · 상태 칩이 보인다 | `entities/learning/ui/lesson/lesson-item.tsx` |
| A9  | 칩 문구는 `학습 완료` 또는 `학습 전` 두 가지다                                           | 〃                                            |
| A10 | 레슨이 0건이면 섹션 제목만 남고 목록은 비어 있다. 빈 상태 안내가 없다                    | `$unitId/index.tsx`                           |
| A11 | 좁은 화면에서는 경로 표시가 사라지고 상단에 챕터명을 제목으로 한 바가 보인다             | `learning/$chapterId/route.tsx`               |

### B. 데이터 조회

| #   | 동작                                                                                     | 확인한 위치         |
| --- | ---------------------------------------------------------------------------------------- | ------------------- |
| B1  | `GET /api/v1/lessons/{unitId}` 로 유닛 요약 · 챕터 요약 · 레슨 목록을 받는다             | `$unitId/index.tsx` |
| B2  | `GET /api/v1/units/{chapterId}` 를 **유닛 순번과 챕터명을 얻으려고** 추가로 부른다       | 〃                  |
| B3  | 둘 중 하나라도 응답이 없으면 **본문 전체가 비어 있다.** 에러 메시지도 재시도 수단도 없다 | 〃                  |

### C. 목적지 4종 (legacy 가 실제로 쓰는 파라미터)

| #   | 목적지      | legacy 경로                                          | 실제로 쓰는 것                            | 셸 유무 |
| --- | ----------- | ---------------------------------------------------- | ----------------------------------------- | ------- |
| C1  | 레슨 풀이   | `/learning/{chapterId}/{unitId}/{lessonId}`          | `lessonId` 만                             | 없음    |
| C2  | 북마크 문제 | `/learning/{chapterId}/{unitId}/bookmarked-problems` | `unitId` 만                               | 없음    |
| C3  | 오답 문제   | `/learning/{chapterId}/{unitId}/incorrect-problems`  | `unitId` 만                               | 없음    |
| C4  | 개념노트    | `/learning/{chapterId}/{unitId}/concept-note`        | `unitId`(내용) · `chapterId`(배너 챕터명) | 있음    |

풀이 화면 3종은 풀이 모드와 문제 수급 방식이 다르다 — 레슨 `LESSON`/`BATCH`,
북마크 `BOOKMARK`/`STREAM`, 오답 `INCORRECT`/`STREAM`.

---

## 동작 계약 (바뀌면 안 되는 것)

| #   | 계약                                                                 | 깨지면 무슨 일이 생기나                                 |
| --- | -------------------------------------------------------------------- | ------------------------------------------------------- |
| K1  | 풀이 화면 3종은 **헤더·하단 탭바가 없는** 레이아웃에서 연다          | 풀이 도중 탭바로 이탈해 진행 중이던 답안이 사라진다     |
| K2  | 레슨 항목을 누르면 그 레슨의 풀이 화면으로 간다                      | 학습 진입 경로가 끊긴다                                 |
| K3  | 유닛 순번은 **챕터 안에서의 순서**다. `unitId` 를 그대로 쓰지 않는다 | 사용자가 보던 유닛 번호가 달라져 다른 유닛으로 오인한다 |
| K4  | 레슨 상태 칩이 학습 완료 여부를 구분해 보여준다                      | 학습 이력이 있는 사용자가 완료 여부를 잘못 읽는다       |
| K5  | 경로 표시의 챕터 항목을 누르면 그 챕터의 유닛 목록으로 돌아간다      | 상위로 올라가는 유일한 경로가 끊긴다                    |

> **legacy 의 URL 구조는 계약이 아니다.** `develop` 이 `refactor(web): 유닛 학습 경로를 단일 ID로
통일` 로 의도적으로 바꿨고, v2 는 아직 출시 전이라 보존할 외부 링크가 없다.

---

## 라우트 계약 (이번에 확정한다)

`develop` 은 `/learning/chapters/{chapterId}` · `/learning/units/{unitId}` 처럼
**리소스 컬렉션 + 단일 ID** 형태로 통일했다. 목적지 4종도 같은 형태로 맞춘다.

`chapterId` 를 경로에 남기지 않아도 된다 — **`GET /api/v1/lessons/{unitId}` 응답이
`chapterSummary: { chapterId, title }` 을 함께 준다.** 경로 표시의 챕터명과 복귀 링크를
유닛 하나로 만들 수 있다.

| 목적지      | 제안 경로                                     | 파라미터   | 레이아웃 그룹 |
| ----------- | --------------------------------------------- | ---------- | ------------- |
| 유닛 상세   | `/learning/units/$unitId`                     | `unitId`   | `_app-shell`  |
| 개념노트    | `/learning/units/$unitId/concept-note`        | `unitId`   | `_app-shell`  |
| 레슨 풀이   | `/learning/lessons/$lessonId`                 | `lessonId` | `_focus`      |
| 북마크 문제 | `/learning/units/$unitId/bookmarked-problems` | `unitId`   | `_focus`      |
| 오답 문제   | `/learning/units/$unitId/incorrect-problems`  | `unitId`   | `_focus`      |

**레슨 풀이가 `/learning/lessons/$lessonId` 인 근거** — legacy 도 `lessonId` 만 쓴다(C1).
단일 ID 통일 방침과 일치하고, 레슨은 유닛과 독립적으로 식별된다.

**`_focus` 그룹** — `fsd-pages.md` §6-1 이 "인증 + 크롬 없음(전체화면)"에 대해
`_app-shell/` 밖 형제로 `_focus/` 를 두고 `createFileRoute` 경로로 URL 을 유지하라고 규정한다.
K1 을 구조로 강제한다.

---

## 시안 대조 결과

> `design-diff` 산출물. Figma `LRN-03` 대조일 2026-09-16.
> 대조한 노드의 자식은 `LRN-03-WEB`(1920×1194) · `LRN-03-MOB`(360×740) **둘뿐이다.**
> 상태 변형 프레임이 없다.

### 칩 — 가장 큰 차이

| #   | 항목        | legacy | 시안                                                           | 판정                      |
| --- | ----------- | ------ | -------------------------------------------------------------- | ------------------------- |
| D1  | 종류        | 2종    | **3종** — `학습 전` · `학습 중` · `학습 완료`                  | 2종 구현 · 3 variant 정의 |
| D2  | `학습 전`   | muted  | 흰 배경 · 테두리 `divider-2`(#c6c6c6) · 글자 `text-4`(#a8a8a8) | 고침                      |
| D3  | `학습 중`   | 없음   | 배경 #9b00cf · 흰 글자 — **채움형**                            | variant 만 정의           |
| D4  | `학습 완료` | filled | 흰 배경 · 테두리·글자 #9b00cf — **외곽선형**                   | 고침                      |
| D5  | 공통 형태   | —      | pill(radius 60) · px 16 · py 4 · 16px Regular                  | 고침                      |

> ⚠️ **채움/외곽선의 의미가 legacy 와 반대다.** legacy 는 채움 = 완료인데 시안은 채움 = 학습 중이다.
> 시안을 따르기로 판정했다 (Figma 가 SoT — `design-source-policy.md` §1).

### 상태 커버리지

| #   | 항목               | legacy          | 시안                                         | 판정            |
| --- | ------------------ | --------------- | -------------------------------------------- | --------------- |
| D6  | `학습 중` 데이터   | `isSolved` 하나 | 3번째 상태 요구 — **현재 API 로 불가능**     | 확인 필요 2     |
| D7  | 북마크·오답 비활성 | 분기 없음       | **비활성 프레임 없음.** API 는 플래그를 준다 | 확인 필요 3     |
| D8  | 레슨 0건 빈 상태   | 안내 없음       | **프레임 없음**                              | 확인 필요 3     |
| D9  | 로딩               | 빈 화면         | **프레임 없음**                              | 고침 — 스켈레톤 |
| D10 | 에러               | 빈 화면         | **프레임 없음**                              | 확인 필요 4     |

### 토큰

| #   | 항목                                                                    | `apps/web` 현재                                  | 시안                                                                            | 판정                 |
| --- | ----------------------------------------------------------------------- | ------------------------------------------------ | ------------------------------------------------------------------------------- | -------------------- |
| D11 | 칩 강조 보라                                                            | `--color-main-2` = **#8100b3 (다름)**            | `brand/Main/2` = #9b00cf                                                        | 값이 같은 `cta` 사용 |
| D12 | 카드 흰 배경                                                            | **`bg-0` 별칭 없음**                             | `bg/0` = #ffffff                                                                | `bg-white` 사용      |
| D13 | 카드 그림자·테두리                                                      | **shadow 토큰 없음**                             | `0 4px 16px rgba(0,0,0,0.02)` · 테두리 `rgba(251,241,255,0.6)`(Figma 도 원시값) | 생략                 |
| D14 | 오답노트 아이콘 색                                                      | `--color-3` = #ffb608 — 대장에 **"미사용"** 분류 | 변수 `3` = #ffb608 — **시안이 쓴다**                                            | 확인 필요 5          |
| D15 | `divider-2` `text-2` `text-3` `text-4` `purple-100` `bg-1` · `text-1-w` | 값 일치                                          | 값 일치                                                                         | 유지                 |

### 구조 · 문구 · 수치

| #   | 항목                                                                                                 | 결과                                                     | 판정 |
| --- | ---------------------------------------------------------------------------------------------------- | -------------------------------------------------------- | ---- |
| D16 | 경로 3단 · 제목 `Unit01`(유닛명 없음) · 설명                                                         | legacy 와 일치                                           | 유지 |
| D17 | 좌 400px 컬럼(개념노트·북마크·오답 세로) + 우 문제 리스트 · 컬럼 간격 24 · 본문 최대 1200            | legacy 와 일치                                           | 유지 |
| D18 | 좁은 화면에서 북마크·오답 가로 2열, 경로 표시 없음                                                   | legacy 와 일치                                           | 유지 |
| D19 | 문구 6종 (`개념노트` `북마크` `오답노트` 설명 2종 `문제 리스트` `{n}문제`)                           | **전부 일치**                                            | 유지 |
| D20 | 개념노트 아이콘                                                                                      | legacy 32px → 시안 48px                                  | 고침 |
| D21 | 카드 설명 글자색                                                                                     | legacy `text-3` → 시안 `text-4`                          | 고침 |
| D22 | 문제 리스트 카드 여백                                                                                | legacy px32/py28/gap24 → 시안 padding 32 전방위 / gap 16 | 고침 |
| D23 | `문제 리스트` 라벨                                                                                   | legacy 카드 제목 스타일 → 시안 16px Regular `text-4`     | 고침 |
| D24 | 레슨 행 높이                                                                                         | legacy 미지정 → 시안 74px 고정                           | 고침 |
| D25 | chevron                                                                                              | legacy 24px → 시안 카드 20px · 경로 16px                 | 고침 |
| D26 | 개념노트 px26/py32 · 북마크·오답 px32/py28 · 아이콘 60px · 레슨 행 px16/py12/radius8 · 카드 radius12 | legacy 와 일치                                           | 유지 |

---

## 확인 필요

> 기획 · Figma · 라우트 · API 계약이 서로 어긋나 **임의로 결정하면 안 되는 것**만 적는다.

1. **유닛 순번(`Unit01`)을 어떻게 얻는가 — 사용자 확인 중.**
   `GET /api/v1/lessons/{unitId}` 응답에 챕터 내 순번이 없다. 세 가지 방법이 있다.

   | 안  | 방법                                                         | 추가 요청    | 비고                                                          |
   | --- | ------------------------------------------------------------ | ------------ | ------------------------------------------------------------- |
   | A   | `unitSummaryResponse.title` 을 그대로 제목으로               | 없음         | 시안의 `Unit01` 이 실제 title 값이면 정답. 실데이터 확인 필요 |
   | B   | `chapterSummary.chapterId` 로 `/units/{chapterId}` 추가 조회 | 1건 (폭포수) | **URL 은 여전히 `unitId` 만** 필요. legacy 와 같은 결과       |
   | C   | 백엔드에 챕터 내 순번 필드 요청                              | 없음         | 가장 깨끗하지만 대기가 생긴다                                 |

   관련 관찰 — `entities/learning/model/unit-progress.ts` 에 이미
   `TODO(API): 챕터 내 순번 필드가 확정될 때까지 1부터 시작하는 목록 순번을 사용한다` 가 있고,
   `UnitCard` 는 `Lesson {formatUnitNumber(unitId)}` 로 **`unitId` 를 그대로 번호로 표시**한다.
   그 유닛이 챕터의 몇 번째인지와 일치하는지 확인이 필요하다 (K3).

2. **`학습 중` 칩 (D3·D6).** `LessonSummaryResponse` 는 `isSolved: boolean` 하나뿐이라 3번째 상태를
   만들 수 없다. 이번에는 2종만 쓰고 `Chip` 에 variant 3개를 정의해 둔다 —
   API 가 필드를 주면 **매핑 한 줄만 바꾸면 된다.**

3. **`bookmarkAccessible` · `wrongAnsweredNoteAccessible` 반영 여부 (D7·D8).** API 는 값을 주는데
   시안에 비활성 프레임이 없다. 지금은 풀 문제가 0건이어도 카드를 눌러 빈 풀이 화면으로 들어가는
   경로가 열려 있다. 레슨 0건 빈 상태도 시안에 정의가 없다.

4. **에러 표시 (D10).** TanStack Router 의 `errorComponent` 는 `__root` 와 라우트 레벨에 걸리는
   전 화면 공통 관심사다. 이 화면 전용 에러 UI 를 만들면 다음 화면이 또 자기 것을 만든다.
   별도 작업으로 다루되, 공통 에러 시안이 있는지 먼저 확인이 필요하다.

5. **토큰 (D11~D14).** `main-2` 값 불일치 · `bg-0` 부재 · shadow 토큰 부재 ·
   `--color-3` 이 정리 대상 대장에 "미사용"으로 잘못 분류돼 있는 것.
   `FIX-007`(색 토큰 5건 — 디자이너 확인 대기)과 **함께 묻는다.** 따로 두 번 묻지 않는다.

6. **화면 ID 매핑.** 이 프레임 이름은 `LRN-03` 인데 `docs/implementation-status.md` 는
   `L.1`~`L.10` 로 관리한다. `INFRA-031-학습-화면-명세-승격` 이 이미 to-do 에 있으므로
   거기서 함께 정리한다. 추측해 연결하지 않는다.

> **1~6 중 구현을 막는 것은 1번뿐이다.** 2·3 은 legacy 동작을 유지하면 되고, 4·5·6 은 별도 작업이다.

---

## 확정 명세 · 검증 기준

> 확인 필요 1이 해결된 뒤 `ai-plan` 단계에서 작성한다.

- [ ] **AC-1** (범위: 단위|통합)
      Given `구체적 데이터값`
      When `관찰 가능한 행동`
      Then `값·개수·문구`

---

## 동일성 확인 방법

- **라우트 진입** — `/learning/units/{unitId}` 를 직접 열어 제목과 `문제 리스트` 섹션이 보이는지
- **이동 4종** — 네 카드/항목을 눌러 위 라우트 계약표의 URL 로 바뀌는지. 뒤로가기로 돌아오는지
- **셸 유무** — 풀이 화면 3종에서 헤더와 하단 탭바가 **보이지 않는지** (K1)
- **레슨 칩** — 이미 푼 레슨과 안 푼 레슨의 칩이 구분되는지 (K4)
- **경로 복귀** — 경로 표시의 챕터 항목이 `/learning/chapters/{chapterId}` 로 가는지 (K5)
- **반응형** — 좁은 화면에서 경로 표시가 사라지고 북마크·오답이 가로 2열이 되는지
- **기존 테스트** — legacy 에 이 화면 테스트는 없다

---

## Changelog

| 날짜       | 요약                                                        | 사유                                                                                    | 연관 항목                           |
| ---------- | ----------------------------------------------------------- | --------------------------------------------------------------------------------------- | ----------------------------------- |
| 2026-09-16 | 작업 생성 · 기준선 · 시안 대조 결과 · 라우트 계약 제안 작성 | 낡은 트리 기준으로 만든 `MIG-026` 을 폐기하고 `develop`(631bcbb) 기준으로 다시 작성했다 | `refactor-baseline` · `design-diff` |

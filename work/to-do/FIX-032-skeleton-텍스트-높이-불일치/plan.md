---
id: 'FEAT-000'
planned: 'YYYY-MM-DD'
mode: 'feature' # feature | migrate | refactor | design-fix | fix | infra
---

# {ID} 구현 계획

> `ai-plan` 산출물. **사용자 승인 전에는 다음 단계(구현)로 넘어가지 않는다.**

## 0. 모드 판정

`mode: {…}` — 근거 한 줄.

### 0-1. 착수 전 필수 게이트 <!-- MIG- / REF- 전용 -->

> 점수로 상쇄하지 않는다. 답과 근거를 설명할 수 없는 항목이 있으면 `spec.md`의 확인 필요에
> 기록하고 `to-do`에 보류한다.

| #   | 질문                                | 답  | 근거 |
| --- | ----------------------------------- | --- | ---- |
| 1   | 목표와 비목표가 명확한가            |     |      |
| 2   | 반복 비용이나 확장 차단이 있는가    |     |      |
| 3   | 보존할 동작의 기준선이 있는가       |     |      |
| 4   | 자동 또는 수동 검증 방법이 있는가   |     |      |
| 5   | 범위를 독립적으로 완료할 수 있는가  |     |      |
| 6   | 위험과 실패 시 복구 방법을 정했는가 |     |      |

### 0-2. 자동 보류 신호 — 하나라도 해당하면 **중단**

- [ ] 동작 변경이 같이 들어감 → `FEAT-` / `FIX-`로 분리
- [ ] 한 단위로 완료·검증할 수 없음 → 실행 가능한 단계로 쪼개기
- [ ] 자동 생성물(`routeTree.gen.ts`, `shared/api/generated/`)을 직접 손봐야 함
- [ ] 기존 검증 실패의 원인·영향 범위를 설명할 수 없음
- [ ] 범위 밖 문제가 섞임 → 별도 작업으로 분리

## 1. 요구사항 분해 (레이어 태그)

| #   | 요구사항 | 레이어                                                             |
| --- | -------- | ------------------------------------------------------------------ |
| 1   |          | `[shared]` / `[entities]` / `[features]` / `[widgets]` / `[pages]` |

### 1-1. 관리 포인트 식별 <!-- FEAT- 전용 -->

상수로 뽑을 값 vs 인라인. **애매하면 인라인이 기본값이다.**
요구사항 본문에 `설정값으로 관리` `관리 포인트` `상수로 관리` `상수로 정의` 중 하나라도 있으면 해석 여지 없이 상수로 추출한다.

| 값  | 상수 / 인라인 | 근거 |
| --- | ------------- | ---- |

## 2. 영향 분석

| 구분 | 파일 |
| ---- | ---- |
| 신규 |      |
| 수정 |      |
| 삭제 |      |

npm 의존성 추가: 없음 / …

### 2-1. 이전 매핑 <!-- MIG- / REF- 전용 -->

| 현재 위치 | 목표 위치 | 변경 종류             | import 영향 |
| --------- | --------- | --------------------- | ----------- |
|           |           | 이동/rename/분해/병합 | N곳         |

## 3. 의존 관계 검증

FSD 위반이 생기는 지점과 대안. 위반이 없으면 `없음`.

## 4. 구현 계획 체크리스트

> **반드시 `shared → entities → features → widgets → pages` 순서.**
> 아래 레이어를 먼저 만들어야 위 레이어가 그걸 import 할 수 있다.

- [ ] `[shared]`
- [ ] `[entities]`
- [ ] `[features]`
- [ ] `[widgets]`
- [ ] `[pages]`

## 5. 리스크

| 리스크 | 영향 | 대응 |
| ------ | ---- | ---- |

## 6. 완료 후 액션

- [ ] 작업 폴더를 `work/in-progress/` → `work/done/`으로 이동
- [ ] 화면 상태를 `docs/implementation-status.md`에 반영 (해당 시)
- [ ] MIG 작업이면 `docs/migration-status.md`에 반영
- [ ] 확정 명세를 `docs/fe-implement-spec/{화면ID}/`로 승격
- [ ] 새로 정한 규칙이 있으면 `.claude/rules/` · `docs/conventions/`에 반영
